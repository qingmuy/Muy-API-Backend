server:
  port: 8090
spring:
  cloud:
    gateway:
      default-filters:
        - AddResponseHeader=source, muy
      routes:
        - id: api_route
          uri: http://localhost:8123
          predicates:
            - Path=/api/**
